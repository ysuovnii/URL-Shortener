<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign up</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif;
            background: #36393f;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #dbdee1;
            padding: 20px;
        }

        .signup-container {
            width: 100%;
            max-width: 420px;
            background: #36393f;
            border-radius: 12px;
            padding: 40px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.5);
            animation: slideIn 0.4s ease-out;
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .brand {
            text-align: center;
            margin-bottom: 30px;
        }

        h1 {
            font-size: 24px;
            font-weight: 600;
            color: #ffffff;
            margin-bottom: 8px;
        }

        .subtitle {
            font-size: 14px;
            color: #949ba4;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            display: block;
            font-size: 12px;
            font-weight: 600;
            color: #b9bbbe;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 8px;
        }

        input[type="email"],
        input[type="text"],
        input[type="password"],
        select {
            width: 100%;
            padding: 12px;
            border: 1px solid #202225;
            border-radius: 4px;
            background: #2f3136;
            color: #dcddde;
            font-size: 14px;
            transition: all 0.2s ease;
        }

        input[type="email"]:focus,
        input[type="text"]:focus,
        input[type="password"]:focus,
        select:focus {
            outline: none;
            border-color: #5865f2;
            background: #2c2f33;
            box-shadow: 0 0 0 2px rgba(88, 101, 242, 0.1);
        }

        input::placeholder {
            color: #72767d;
        }

        .password-note {
            font-size: 12px;
            color: #72767d;
            margin-top: 4px;
        }

        .checkbox-group {
            display: flex;
            align-items: flex-start;
            margin-bottom: 24px;
            gap: 12px;
        }

        input[type="checkbox"] {
            width: 18px;
            height: 18px;
            margin-top: 2px;
            cursor: pointer;
            accent-color: #5865f2;
            flex-shrink: 0;
        }

        .checkbox-label {
            font-size: 13px;
            color: #b9bbbe;
            cursor: pointer;
            line-height: 1.4;
            text-transform: none;
            font-weight: 400;
            letter-spacing: normal;
            text-transform: none;
        }

        .checkbox-label a {
            color: #00b0f4;
            text-decoration: none;
        }

        .checkbox-label a:hover {
            text-decoration: underline;
        }

        .form-error {
            color: #f04747;
            font-size: 13px;
            margin-top: 4px;
            display: none;
        }

        .form-error.show {
            display: block;
        }

        .btn-signup {
            width: 100%;
            padding: 12px;
            background: #5865f2;
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
            margin-bottom: 20px;
        }

        .btn-signup:hover {
            background: #4752c4;
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(88, 101, 242, 0.3);
        }

        .btn-signup:active {
            transform: translateY(0);
        }

        .btn-signup:disabled {
            background: #72767d;
            cursor: not-allowed;
            opacity: 0.5;
        }

        .login-link {
            text-align: center;
            font-size: 13px;
            color: #72767d;
        }

        .login-link a {
            color: #00b0f4;
            text-decoration: none;
            font-weight: 600;
        }

        .login-link a:hover {
            text-decoration: underline;
        }

        .footer-links {
            text-align: center;
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #2f3136;
            font-size: 12px;
        }

        .footer-links a {
            color: #72767d;
            text-decoration: none;
            margin: 0 12px;
            transition: color 0.2s ease;
        }

        .footer-links a:hover {
            color: #b9bbbe;
        }

        @media (max-width: 480px) {
            .signup-container {
                padding: 30px 20px;
            }

            h1 {
                font-size: 20px;
            }

            .logo {
                font-size: 28px;
            }
        }
    </style>
</head>
<body>
    <div class="signup-container">
        <div class="brand">
            <h1>Create an account</h1>
            <p class="subtitle">We're so excited to see you here!</p>
        </div>

        <% if (error) { %>
            <div style="background: #40444b; border-left: 4px solid #f04747; padding: 12px; border-radius: 4px; margin-bottom: 20px; font-size: 13px; color: #f04747;">
                <%= error %>
            </div>
        <% } %>

        <form action="/user/signup" method="POST" id="signupForm">
            <div class="form-group">
                <label for="email">Email Address</label>
                <input 
                    type="email" 
                    id="email" 
                    name="email" 
                    placeholder="you@example.com" 
                    required
                >
                <div class="form-error"></div>
            </div>

            <div class="form-group">
                <label for="username">Username</label>
                <input 
                    type="text" 
                    id="username" 
                    name="username" 
                    placeholder="cooluser123" 
                    minlength="2"
                    maxlength="32"
                    required
                >
                <div class="form-error"></div>
            </div>

            <div class="form-group">
                <label for="password">Password</label>
                <input 
                    type="password" 
                    id="password" 
                    name="password" 
                    placeholder="••••••••" 
                    minlength="8"
                    required
                >
                <p class="password-note">Must be at least 8 characters</p>
                <div class="form-error"></div>
            </div>

            <div class="form-group">
                <label for="confirmPassword">Confirm Password</label>
                <input 
                    type="password" 
                    id="confirmPassword" 
                    name="confirmPassword" 
                    placeholder="••••••••" 
                    required
                >
                <div class="form-error"></div>
            </div>

            <button type="submit" class="btn-signup">Create Account</button>

            <div class="login-link">
                Already have an account? <a href="/user/login">Login</a>
            </div>
        </form>

        <div class="footer-links">
            <a href="#">Terms</a>
            <a href="#">Privacy</a>
            <a href="#">Support</a>
            <a href="#">Blog</a>
        </div>
    </div>

    <script>
        document.getElementById('signupForm').addEventListener('submit', function(e) {
            const password = document.getElementById('password').value;
            const confirmPassword = document.getElementById('confirmPassword').value;

            if (password !== confirmPassword) {
                e.preventDefault();
                document.getElementById('confirmPassword').nextElementSibling.textContent = 'Passwords do not match';
                document.getElementById('confirmPassword').nextElementSibling.classList.add('show');
            }
        });

        // Clear error on input
        document.querySelectorAll('input').forEach(input => {
            input.addEventListener('input', function() {
                this.nextElementSibling?.classList.remove('show');
            });
        });
    </script>
</body>
</html>
